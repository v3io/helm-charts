apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "scaler.scaler.name" . }}
  labels:
    app: {{ template "scaler.name" . }}
    chart: {{ template "scaler.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: scaler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "scaler.name" . }}
      chart: {{ template "scaler.chart" . }}
      release: {{ .Release.Name }}
      heritage: {{ .Release.Service }}
      component: scaler
  template:
    metadata:
      labels:
        app: {{ template "scaler.name" . }}
        chart: {{ template "scaler.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
        component: scaler
    spec:
      containers:
        - name: scaler
          image: {{ .Values.scaler.image.repository }}:{{ .Values.scaler.image.tag }}
          imagePullPolicy: {{ .Values.scaler.image.pullPolicy }}
          workingDir: "{{ .Values.config.pathMount }}"
          command: ["{{ .Values.config.command }}"]
          env:
            - name: SCALER_NAMESPACE
              value: {{ .Release.Namespace }}
            - name: KUBECONFIG
              value: ""
          args:
            - --namespace={{ .Release.Namespace }}
            - --scale-interval={{ .Values.scaler.scaleInterval }}
            - --scale-window={{ .Values.scaler.scaleWindow }}
            - --metrics-poll-interval={{ .Values.scaler.metricsPollInterval }}
            - --metric-name={{ .Values.scaler.metricName }}
            - --scale-threshold={{ .Values.scaler.scaleThreshold }}
