{{- if .Values.rbac.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "persistenceagent.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "persistenceagent.name" . }}
    chart: {{ template "pipelines.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- end }}
