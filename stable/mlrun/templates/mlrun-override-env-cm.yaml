{{- if .Values.api.persistence.enabled -}}
apiVersion: v1
data:
  MLRUN_ARTIFACT_PATH: "/home/jovyan/data"
kind: ConfigMap
metadata:
  annotations:
  labels:
    {{- include "mlrun.db.labels" . | nindent 4 }}
  name: mlrun-override-env
{{- end -}}
