# Default values for tenant.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

name: iguazio

tiller:
  serviceAccountSuffix: tiller

nodeSelector: {}

tolerations: []

affinity: {}
