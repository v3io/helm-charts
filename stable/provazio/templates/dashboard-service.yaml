{{- if .Values.dashboard.enable }}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "provazio.dashboard.name" . }}
  labels:
    app: {{ template "provazio.dashboard.name" . }}
    chart: {{ template "provazio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - name: admin
    port: 8060
    protocol: TCP
    targetPort: 8060
  selector:
    provazio/service: {{ template "provazio.dashboard.name" . }}

{{- end }}
