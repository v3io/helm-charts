apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-gpu-metrics-exporter.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "pod-gpu-metrics-exporter.name" . }}
    helm.sh/chart: {{ include "pod-gpu-metrics-exporter.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pod-gpu-metrics-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
