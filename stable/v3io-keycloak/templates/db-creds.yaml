apiVersion: v1
kind: Secret
metadata:
  name: v3io-keycloak-db-creds
  labels:
    {{ include "v3io-keycloak.labels" . | nindent 4 }}
data:
  DB_USER: {{ include "v3io-keycloak.dbUser" . | b64enc }}
  DB_PASSWORD: {{ include "v3io-keycloak.dbPassword" . | b64enc }}
