{{- if .Values.pipelines.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
  labels:
    application-crd-id: kubeflow-pipelines
  name: ml-pipeline-viewer-crd-service-account
secrets:
- name: ml-pipeline-viewer-crd-service-account-token-txp9k
{{- end -}}
