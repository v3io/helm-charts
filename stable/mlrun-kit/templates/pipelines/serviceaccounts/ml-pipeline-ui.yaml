{{- if .Values.pipelines.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
  labels:
    application-crd-id: kubeflow-pipelines
  name: ml-pipeline-ui
secrets:
- name: ml-pipeline-ui-token-gnw2b
{{- end -}}
