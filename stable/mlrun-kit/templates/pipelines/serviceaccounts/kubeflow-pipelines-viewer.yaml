{{- if .Values.pipelines.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
  labels:
    application-crd-id: kubeflow-pipelines
  name: kubeflow-pipelines-viewer
secrets:
- name: kubeflow-pipelines-viewer-token-zg54c
{{- end -}}
