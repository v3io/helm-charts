# Default values for tenant.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
image:
  repository: iguaziodocker/flex-docker
  tag: 1.0.0
  pullPolicy: IfNotPresent

nodeSelector: {}

tolerations: []

affinity: {}
